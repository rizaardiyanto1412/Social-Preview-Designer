{
	"name": "wp-remote-work/wp-remote-og-plugins",
	"description": "Design one Open Graph image template and automatically generate a branded social preview image for every post.",
	"type": "wordpress-plugin",
	"version": "1.0.0",
	"license": "GPL-2.0-or-later",
	"require": {
		"php": ">=7.4"
	},
	"scripts": {
		"build:zip": "wp dist-archive . ./dist"
	}
}
